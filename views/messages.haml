.row
  .small-12.small-centered.columns
    .row
      - @messages.each do |message|
        .small-6.large-3.columns
          .panel.radius
            %p= message.name
            %h5= message.body
            %small= message.time.strftime("%B %d at %I:%M %p")